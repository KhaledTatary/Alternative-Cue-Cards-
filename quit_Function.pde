void quit_Function() {
   if (mouseX > width*7/8 && mouseX < width && mouseY > height*0 && mouseY < height) {
    
    
  } 
  else {
    colorC = colorA;
  }
  fill(colorMix);
  rect(width*7/8, height*0, width*2/16, height*1/12);
}